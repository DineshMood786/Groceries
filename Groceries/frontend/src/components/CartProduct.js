import React from "react";

const CartProduct = ({ productName, category, image, price, description }) => {
  console.log("Cart Product page is working");
  return <div>{productName}</div>;
};

export default CartProduct;
